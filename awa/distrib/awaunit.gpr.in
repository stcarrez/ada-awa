with "util";
with "testutil";
with "el";
with "asf";
with "asfunit";
with "ado";
with "awa";

project AwaUnit is
   Version := "@AWA_VERSION@";

   for Library_Name use "awaunit";
   for Library_Dir use "INSTALL/lib";
   for Library_Kind use "LIBRARY_TYPE";

   for Source_Dirs use ("INSTALL/share/ada/adainclude/awaunit");
   for Library_ALI_Dir use "INSTALL/lib/ada/adalib/awaunit/" & Project'Library_Kind;
   for Externally_Built use "true";
end AwaUnit;
